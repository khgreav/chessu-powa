/**
 * Contains enumeration of a chess piece type.
 * Author(s): Michal Bucher (xbuche01), Karel Hanák (xhanak34)
 */

package Pieces;

/**
 * An enumeration of chess piece types.
 */
public enum PieceType {
    KI, Q, B, KN, R, P
}
